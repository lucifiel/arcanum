 image: node:latest

    stages:
      - install
      - ci-dev

    cache:
      paths:
        - node_modules/

    install_dependencies:
      stage: install
      only: master
      script:
        - npm install
      artifacts:
        paths:
          - node_modules/

    main_build:
      stage: ci-dev
      only: master
      script: npm ci-dev