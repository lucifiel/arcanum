 image: node:latest

    stages:
      - install
      - ci-dev

    cache:
      paths:
        - node_modules/

    install_dependencies:
      stage: install
      script:
        - npm install
      artifacts:
        paths:
          - node_modules/

    main_build:
      stage: ci-dev
      script: npm ci-dev