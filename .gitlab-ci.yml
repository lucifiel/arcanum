image: node:latest

stages:
  - install
  - ci-dev

cache:
  paths:
  - node_modules/

install_dependencies:
  stage: install
  only:
    - master
  script:
  - npm install

main_build:
  stage: ci-dev
  only:
    - master
  script: npm ci-dev
  artifacts:
    paths:
      - dev/